import 'package:flutter/material.dart';

class MonthWiseAttendance extends StatelessWidget {
  const MonthWiseAttendance({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text("Select Month")
      ],
    );
  }
}