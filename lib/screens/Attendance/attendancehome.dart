import 'package:flutter/material.dart';

class AttendanceHome extends StatelessWidget {
  const AttendanceHome({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Attendance Home screen"),
    );
  }
}
